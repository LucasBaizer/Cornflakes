package cornflakes.lang;
public class Test;

public static func test() {
	var x: (i32, i32, i32) = (5, 5, 5)
	
	var tuple = functionThatTakesTuple(x)
	foreach y in tuple {
		println!(y)
	}
}

public static func functionThatTakesTuple(tuple: (i32, i32, i32)) -> (i32, i32, i32) {
	return (5, 6, 7)
}
