package cornflakes.lang;
public class Test;

public static func test() {
	var x: (i32, i32, i32) = (5, 5, 5)
	
	println!(functionThatTakesTuple(x))
}

public static func functionThatTakesTuple(tuple: (i32, i32, i32)) -> (i32, string, i32) {
	println!(tuple)
	
	return (5, "hello", 7)
}
